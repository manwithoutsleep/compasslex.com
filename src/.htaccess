# Generated with ngx-htaccess-generator v1.0.1
# https://julianpoemp.github.io/ngx-htaccess-generator/
# [NC] = No Case (i.e. case insensitive)
# [L]  = Last (i.e. don't process any more commands)

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirection of API requests
    RewriteRule ^api/public/counselor$ /api/public/index.php [NC,L]
    RewriteRule ^api/public/newsletter$ /api/public/index.php [NC,L]

    # Redirection of requests to index.html
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f [OR]
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -d
    RewriteRule ^.*$ - [NC,L]

    RewriteRule ^(.*) index.html [NC,L]
</IfModule>